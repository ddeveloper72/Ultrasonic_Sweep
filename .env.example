# Environment Variables for Heroku Deployment
# These are set in Heroku Config Vars, not committed to git

# PORT is automatically set by Heroku
# PORT=5000

# Force single worker to avoid multi-worker memory issues
# WEB_CONCURRENCY=1

# YouTube cookies for bypassing bot detection (Heroku only)
# Export your YouTube cookies.txt file, then paste the entire content here
# Use single-line format with \n for newlines, or use base64 encoding
# Example: heroku config:set YOUTUBE_COOKIES="$(cat cookies.txt)" -a ultrasonic-sweep
# YOUTUBE_COOKIES=

# Add any other environment variables here as needed
# FFMPEG_PATH=/usr/bin/ffmpeg  # Heroku buildpack will install FFmpeg
